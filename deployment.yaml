apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gitops-demo
  name: gitops-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitops-demo
  strategy: {}
  template:
    metadata:
      labels:
        app: gitops-demo
    spec:
      containers:
      - image: rancher/hello-world
        name: hello-world
        resources: {}
status: {}